#!/bin/bash
#!/ruta/a/minishell

bash -c 'set -e; comando1; comando2'

./minishell -c 'set -e; comando1; comando2'

tengo que probar más ctrl \ dentro de un comando, ej cat. porque la respuesta no se ajusta

cat < noexiste 2>&1 > out.txt

echo test &

sleep 1 & echo done


cat << EOF
linea1
linea2
EOF

minishell$ echo \"escapado\"
\ escapado\

echo *

